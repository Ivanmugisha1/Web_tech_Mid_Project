package com.example.mymidexam.interfaces;

import com.example.mymidexam.modal.Teacher;

public interface Teachable {
    // Define methods for teaching a course
    // For example:
    void assignTeacher(Teacher teacher);
    Teacher getAssignedTeacher();
}
